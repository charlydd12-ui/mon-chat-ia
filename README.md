# mon-chat-ia
cd ~/Desktop/mon-chat-ia        # va dans ton dossier
git init                         # initialise git
git add .                        # ajoute tous les fichiers
git commit -m "Premier commit"   # crée un commit
git branch -M main               # crée la branche principale
git remote add origin https://github.com/TON_PSEUDO/mon-chat-ia.git
git push -u origin main          # envoie sur GitHub
